{
  "name": "amlchecker",
  "version": "1.0.0",
  "description": "Insert your transactionID, specify the backhops and get all the addresses associated with your Output within the last n backhops! Uses the mempool.space-API to get the data.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-win": "electron-packager . amlchecker --platform=win32 --arch=x64 --out=dist --overwrite",
    "package-linux": "electron-packager . amlchecker --platform=linux --arch=x64 --out=dist --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^25.0.0",
    "electron-packager": "^17.1.1"
  }
}

